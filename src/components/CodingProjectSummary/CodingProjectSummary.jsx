import './codingProjectSummary.css';

const CodingProjectSummary = (props) => {
    return (
        <p className='coding_project_summary'>{props.summary}</p>
    )
};

export default CodingProjectSummary;